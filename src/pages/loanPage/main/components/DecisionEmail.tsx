import { words } from '../../../../lang/lang';

export const DecisionEmail = () => {
  return (
    <section className="credit-card-decision-email">
      <h2>{words.loanPage.processGetCreditCard.UK.decisionEmail.header}</h2>
      <p>{words.loanPage.processGetCreditCard.UK.decisionEmail.title}</p>
    </section>
  );
};
